# 문제 : 설정에 관한 문제
#   - local, dev, prod에 관한 설정이 존재 하지 않고 있는걸로 보임.
# 원인 : 설정
#   - 개발 편의성을 우선하여 Profile 기반 설정 전략을 적용하지 않고 있다.
# 개선안
# 1. Spring Profile을 활용하여 환경별 설정 파일을 분리하는 것이 올바르다고 판단된다.
#   1-1. application-local, application-dev, application-prod
# 2. 환경별로 달라지는 항목(DB, JPA 옵션 등)은 Profile별로 관리하여 설정 변경에 따른 영향 범위를 최소화하는 것이 바람직 하다고 판단된다.
spring.application.name=code-test

# --- H2 DataSource ---
spring.datasource.url=jdbc:h2:mem:codetest;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# --- H2 Console ---
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# --- JPA / Hibernate ---

# 문제 : 설정에 관한 문제
#   1. JPA 설정(ddl-auto, show-sql 등)이 환경 구분 없이 동일하게 적용되고 있다.
#   2. 운영 환경에서도 개발 환경용 설정이 적용될 위험이 크게 존재한다.
# 원인 : 설정
#   - 환경별로 분리 관리해야 할 JPA 설정을 단일 설정 파일에서 공통으로 관리하고 있다.
# 개선안 : Spring Profile을 사용하여 JPA 설정을 환경별로 분리한다. 이를 통해 환경별 특성에 맞는 설정을 적용하고, 운영 리스크를 최소화 하고 예방해야 된다고 판단된다.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# 문제 : 설정에 관한 문제
#   - H2를 MODE=MySQL로 사용하면서 H2Dialect를 명시하고 있어, 실제 MySQL 환경과의 SQL 동작 차이로 인한 이슈가 발생할 수 있습니다.
# 원인 : 설정
#   - 테스트 DB와 운영 DB 간 호환성에 대한 명확한 기준 및 전략 부재
# 개선안 : 테스트 목적이라면 H2 전용 설정임을 명확히 하고, Dialect 사용 범위를 제한하는 것이 바람직하다고 판단된다.
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# --- SQL init (disable if you don?t have schema.sql/data.sql) ---
spring.sql.init.mode=never
